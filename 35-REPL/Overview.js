/**
 * NodeJS used Javascript engine to run.
 * The JS Engine which Node used is v8 Engine 
 * which is also used by google chrome.
 * 
 * Release : 2009
 * Current Version : 16.9
 * Code Written : C, C++ , Major (Javascript)
 * v8 Engine is made on C++.
 * 
 * Used to develop API in backend .
 * NodeJS is not a language but a runtime environment.
 * Node is not a framework nor a language but a run time open source development
 * platform for executing JavaScript code server-side.
 * NodeJS creates single threaded ASYNC application.(It will run one command at a time).
 * 
 * 💃 Difference between JS and Node : 
 * ---------------------------------
 * 
 * 1) we cannot connect DB to JS code but with node it is possible.
 * 2) Node JS run on server side whereas run on browser.
 * 3) We cannot use import something from other file using import statement in node
 *    but same is possible in JS frameworks like react, angular.
 * 
 * 💃 Similarity : 
 * ------------
 * 1) Codebase is same.
 * 
 * 📓 why browser give sometime undefined.
 * -----------------------------------------------
 * It is because the statment itself didnt return anything.
 * console is a module in nodejs which is similar to browser console but not the same.
 * 
 * 
 * 
 * 
 * 
 * 
 * 💃 What is Client side and server side
 * -----------------------------------------------
 * 
 * In Simple language Server side means the language which to store data in db.
 * Now with Javascript we cannot store data into db, so it is client side.
 * 
 * 
 * 
 */